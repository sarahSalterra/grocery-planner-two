# Pre-Publishing To-Do

## Before Publishing

- [ ] Add ESLint — install the package, create a config, and add a `lint` script to `package.json`.
      Catches null-access bugs, stale hook dependencies, and unused imports at write-time
      rather than at runtime. Ask the assistant to set this up when ready.

- [ ] Update README.md — replace the placeholder "Tech Stack: To be determined" and
      "Setup coming soon" sections with the actual stack (Vite + React 18 + React Router 6,
      localStorage-backed, no backend) and real setup instructions (`npm install` / `npm run dev`).

## Content & Recipes

- [ ] Proofread macaron recipe — check ingredients, quantities, and steps for accuracy.

- [ ] Add buttermilk biscuit recipe and other recipes that use short-shelf-life ingredients
      (e.g. buttermilk, fresh herbs, produce) so those items don't go to waste.

- [ ] Add recipe credit text — attribute sources/authors for recipes that need it.

## Design

- [ ] Adjust visual scheme — revisit colors, typography, or overall look and feel.
